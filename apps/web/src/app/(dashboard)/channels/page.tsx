import { AddChannelSetup } from "@/components/setup/add-channel-setup";

export const metadata = { title: "Channels" };

export default function ChannelsPage() {
  return <AddChannelSetup embedded />;
}
