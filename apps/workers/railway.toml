[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/workers/Dockerfile"
watchPatterns = ["apps/workers/**", "packages/monitoring/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
